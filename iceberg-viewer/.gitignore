# Shallow clone of https://github.com/ytsaurus/ytsaurus-ui — not ours to vendor.
# Re-create with: git clone --depth 1 https://github.com/ytsaurus/ytsaurus-ui.git
ytsaurus-ui/

# Raw Playwright HAR (huge; the extracted browser-traffic.jsonl is committed instead)
recordings/*.har
